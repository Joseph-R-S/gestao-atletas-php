{% extends "layout.html.twig" %}
{% block title %}Dados de Atleta Para TMB
{% endblock %}

{% block content %}
	{{ dump(atleta) }}
	{#
	<form method="POST" action="index.php?class=AvalicaoForm&method=edit&id={{ atleta.id }}">
	    <input type="hidden" name="id" value="{id}">
	
	    <div class="campo">
	        <label>Atleta</label>
	        <input type="text" name="atleta" value="{nome_completo}" required readonly>
	    </div>
	
	    <div class="campo">
	        <label>Peso (kg) <span>*</span></label>
	        <input type="number" step="0.01" name="peso" value="{peso}" required>
	    </div>
	
	    <div class="campo">
	        <label>Altura (m) <span>*</span></label>
	        <input type="number" step="0.01" name="altura" value="{altura}" required>
	    </div>
	
	    <div class="campo">
	        <label>Meta Específica</label>
	        <textarea name="meta_especifica" class="input-textarea">{meta_especifica}</textarea>
	    </div>
	
	    <div class="campo">
	        <label for="intensidade">Intensidade (1 a 10):</label>
	        <input type="number" name="intensidade" min="1" max="10" value="{{ avaliacao.intensidade }}">
	    </div>
	
        <div class="campo">
            <label for="dias_treino_semana">Dias de treino por semana:</label>
            <input type="number" name="dias_treino_semana" min="0" max="7" value="{{ avaliacao.dias_treino_semana }}">
        </div>
	
	    <div class="campo">
	        <button type="submit" class="btn-salvar">Salvar</button>
	    </div>
	</form>
	#}
{% endblock %}
